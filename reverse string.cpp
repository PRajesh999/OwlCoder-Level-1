#include<bits/stdc++.h>
using namespace std;
int main()
{
    string a;
    getline(cin,a);
    int b;
    b=a.length();
    while(b--)
    {
        cout<<a[b];
    }
    return 0;
}

