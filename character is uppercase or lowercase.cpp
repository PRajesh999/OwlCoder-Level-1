#include <bits/stdc++.h>
using namespace std;
int main()
{
    char ch;
    cin>>ch;
    if(ch>=65 and ch<=90) cout<<"uppercase alphabet";
    else if(ch >=97 and ch <=122) cout<<"lowercase alphabet";
    else cout<<"not an alphabet";
}
